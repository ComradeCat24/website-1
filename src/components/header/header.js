import React from 'react';

const Header = () => {
    return (
        <header>
            <div className="logo">
                <span>iFrame as SSO</span>
            </div>
            <div className="episode">
                <span>logout</span>
            </div>
        </header>
    );
};

export default Header;
